# Optional — only needed for social media publishing
LATE_API_KEY=

# Optional — only needed for AI thumbnail generation
KIE_API_KEY=

# Optional — only needed for word-level transcription via API
ELEVENLABS_API_KEY=
